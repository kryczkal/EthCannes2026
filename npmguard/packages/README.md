# `packages/`

Workspace packages that are meant to be used directly by users or demos.

Current package:

- `sginstall/`: CLI that resolves the audited ENS version name, reads audit metadata, downloads the exact IPFS tarball, verifies the CID, and extracts it locally
